#include <stdio.h>
#include <stdlib.h>
int i ;
int bc(char *arr, int size, int lno, int subscript, int lowerbound, int upperbound){
if(subscript<size && subscript>=0) return subscript;
else {fprintf(stderr, "subscript (%d) out of bounds for array %s[%d:%d] at line %d\n", subscript, arr, lowerbound, upperbound, lno);exit(1);}}
main() 
{
int 
x_a
[
 10
];
int x_a_size=10, a_e_upperbound=10, a_e_lowerbound=1, a_linenumber=1 ;
char *a_name="a";
for (i = 0; i < x_a_size; i++) x_a[i]=4444;
int 
x_k
=8888;
for(
x_k
=
2
;
x_k
<=
30
;
x_k
++)
{
log: arrFactor: k
log: arrFactor: 
log: arrFactor: 4444
bc("a", 10, 1, , 1, 10);
x_a[

 - 1]
=
x_k
;
}
}
